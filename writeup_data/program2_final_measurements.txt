Program 2 final measurement pass, 5 repetitions, N=10000, VECTOR_WIDTH=4, on Intel i5-7200U:

All 5 runs: Vector Utilization = 83.6%, Total Vector Instructions = 102070,
Result Verification = Passed, every single run identical.

Note: Unlike Programs 1/3/4 (wall-clock speedup), Program 2's reported metric
is vector utilization, which is deterministic given the same input sequence
(exponents/values are generated by rand() without an explicit seed, so each
fresh process run produces the identical pseudo-random sequence by default).
5 repetitions confirm full stability/reproducibility of this result.
